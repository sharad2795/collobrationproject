<!DOCTYPE html>
<html>
<head>
<meta charset="ISO-8859-1">
<title>Insert title here</title>
</head>
<body>
hi
<div ng-controller="forumcontroller as ctrl">
<div ng-controller="blogcontroller as bctrl" ng-show="blogs.length">
<div ng-hide="blogs.length"><center><h1>No Blogs To Display</h1></center></div>
<div class="row">
<div ng-repeat="forum in forums">
    <div class="col-xs-12 col-md-12">
      <div class="thumbnail" id="bg">
          <div class="caption">
          <center><h1>{{forum.topic}}</h1></center>
          <h4>Author : {{forum.userid}}</h4>
          <h4>DateCreated : {{forum.dateadded}}</h4>
          <h4>Comments:<p><div ng-repeat="comment in comments" ng-if="forum.id == comment.forumid">{{comment.userid}} : {{comment.message}}</div></p></h4>
          <a ng-hide="currentUser.id" href="#/login" ng-click="bctrl.forumcomment(forum.id)" class="btn btn-success">Add Comments</a>
          <a ng-show="currentUser.id" href="#/createnewcomment" ng-click="bctrl.forumcomment(forum.id)" class="btn btn-success">Add Comments</a>
          <a href="#" ng-if="forum.userid==currentUser.id" ng-click="ctrl.update(forum.id)"><span class="glyphicon glyphicon-pencil btn btn-success">&nbsp;Edit</span></a>
          <a href="#" ng-if="forum.userid==currentUser.id" ng-click="ctrl.remove(forum.id)"><span class="glyphicon glyphicon-trash btn btn-warning">&nbsp;Delete</span></a>
          </div>
          </div>
          </div>
    </div>
    </div>
 
</div>
</div>
</body>
</html>