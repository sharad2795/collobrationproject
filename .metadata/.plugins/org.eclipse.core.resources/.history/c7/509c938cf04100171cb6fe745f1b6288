<html>
<head>

    
  </head>
  <body >
  <div ng-controller="ChatController">
  <h2><strong>Welcome to our one-and-only group chat Hi!</strong></h2>
  <p>Wanna chat with new people and engage in a small-talk or a sober conversation,... Then let us all chat here for a while to get along!</p>
  <p>Just type in the text, and hit send!</p>
  <div class="w3-animate-opacity">
      <form ng-submit="addMessage()" name="messageForm">
        <input type="text" placeholder="Compose a new message..." ng-model="message" />
        <div class="info">
          <span class="count" ng-bind="max - message.length" ng-class="{danger: message.length > max}">140</span>
          <button ng-disabled="message.length > max || message.length === 0">Send</button>
        </div>
      </form>
      <hr />
      <p ng-repeat="message in messages | orderBy:'time':true" class="message">
        <time>{{message.time | date:'HH:mm'}}</time>
        <span ng-class="{self: message.self}">{{message.message}}</span>
        {{message.message}}
      </p>
</div>
   </div>
</body>
</html>